# 项目进度

## 新对话恢复提示词

```text
请继续协助我完成“基于 CIFAR-10 子集的表示构建与聚类分析”课堂作业。

以当前打开的 Git 仓库为项目根目录，不假定固定盘符或用户名。开始前依次阅读：
1. AGENTS.md：项目范围、长期协作规则、已确认方案与实施策略。
2. 项目进度.md：当前阶段、讨论分析、实际结果和下一步。
3. README.md，以及当前任务指向的原始要求、代码或结果。

依据实验要求.pdf 和用户已确认要求推进；区分原文要求、已确认方案、未验证设想和实际结果。核对实际文件与必要的 Git 状态，不因对话摘要不完整而重做已经完成的工作。

遵循最小充分方案，只完成当前明确任务，不额外建立哈希、审计、门禁、备份体系或测试框架。交付文件的必要备份按 AGENTS.md 执行，正式文件名保持稳定。

用适合初学者的中文说明目的、输入输出和关键概念；正式实验前说明比较因素、评价方法和结论边界。已授权且明确的工作直接继续，关键要求缺失时再询问。

AGENTS.md 只保存稳定的方案与实施策略；方法分析、方案评价、未验证假设、报告讨论素材、阶段结果和下一步放在本文件。

任务结束或出现重要结果后更新本文件，记录结果路径、必要验证、明确问题和下一步；不把计划或仅本地提交写成已经完成的实验或远程推送。
```

> 更新时间：2026-09-11

## 当前阶段

Basic 方案讨论已基本闭环，已经进入第一版代码实现阶段。`src/run_basic.py` 已建立，覆盖数据抽样、三种表示、PCA、k-means 的 k 扫描、最终指标、二维可视化、代表样本和真实类别组成辅助分析。

当前尚未在用户实际运行环境中下载 CIFAR-10 并完成正式实验，因此不存在真实 SSE、Silhouette、CH、DB、时间、内存或最终 k 结果。下一步先运行第一次 `k=2~12` 扫描，根据真实曲线确定三种表示各自的最终 k，再运行最终聚类阶段。

## 已确认选择

- 本项目定位为课堂作业，采用最小充分方案。
- 仓库：<https://github.com/mengmcy1/cifar10-clustering>，私有仓库，默认分支 `main`。
- `AGENTS.md` 只保存长期规则、固定方案和实施策略；本文件保存实时进度、分析评价、报告素材、未验证假设与结果。
- 数据使用 CIFAR-10 的 `airplane`、`automobile`、`frog`、`cat`、`dog` 五类，每类 600 张，共 3000 张；三种表示使用完全相同的样本。
- 第一版实现从 CIFAR-10 train split 中按 `random_state=42` 抽样，并导出 `results/sample_list.csv`。
- 主实验分别使用 Raw Pixel、HSV Color Histogram、HoG 三种表示，不在主实验开始时拼接。
- 三种表示分别独立做 PCA-50D 用于聚类、PCA-2D 用于可视化，不统一追加 `StandardScaler`，不使用 PCA whitening。
- k-means 扫描 `k=2~12`，固定 `init='k-means++'`、`n_init=20`、`random_state=42`。
- 最终 k 必须结合 SSE 肘部和 Silhouette-k 曲线判断，不因为真实类别数为 5 而直接固定 `k=5`。
- 真实类别标签不参与特征构建、PCA 或聚类，只用于抽取指定类别以及聚类后的解释和辅助分析。
- Plus 阶段延后到 Basic 全部完成后；届时三个 Plus 方向都可以尝试，再根据真实结果决定报告重点。

## 已完成

- 创建并初始化 `AGENTS.md`、`README.md`、`项目进度.md`、`.gitignore`，保留原始 `实验要求.pdf`。
- 完整阅读 `实验要求.pdf` 两页内容，确认 Basic 主线包括：5 类 CIFAR-10 子集、表示构建、PCA-50D / PCA-2D、k-means `k=2~12`、SSE 与 Silhouette 选 k、二维可视化、每簇 3 张代表样本、Silhouette / CH / DB、运行时间与内存；Plus 三选一为最低要求。
- 确定 Raw Pixel：展平 3072D，像素缩放到 `[0,1]`。
- 确定 HSV Histogram：H/S/V 各 32 bins，共 96D；各通道分别 L1 归一化。
- 确定 HoG：灰度图，`orientations=9`、`pixels_per_cell=(4,4)`、`cells_per_block=(2,2)`、`block_norm='L2-Hys'`。
- 确定 PCA：每种表示独立拟合 50D 和 2D；不额外 `StandardScaler`；`whiten=False`；记录 PCA-50 累计解释方差比例。
- 确定 k-means 基本参数和 k 扫描策略。
- 新增 `requirements.txt`，包含当前 Basic 所需的 NumPy、pandas、Matplotlib、scikit-learn、scikit-image、psutil、PyTorch / torchvision。
- 新增 `src/run_basic.py`，实现第一版 Basic 主流程。
- 更新 `README.md`，补充环境安装、首次 k 扫描和最终聚类两阶段运行方式。

## 第一版代码行为

### 第一次运行：k 扫描

执行：

```bash
python src/run_basic.py
```

脚本将：

1. 自动下载/读取 CIFAR-10 train split。
2. 固定随机种子，从五个类别各抽 600 张，并输出 `results/sample_list.csv`。
3. 对完全相同的 3000 张图分别构建 Raw Pixel、HSV Histogram、HoG。
4. 每种表示分别拟合 PCA-50D 与 PCA-2D，并记录 PCA-50 累计解释方差。
5. 在 PCA-50D 上扫描 `k=2~12`，保存每个 k 的 SSE 与 Silhouette。
6. 输出每种表示的 `k_scan.csv` 与 `k_scan.png`。
7. 控制台只给出 Silhouette 单指标最高的 k 作为参考，不自动把它写成最终 k；正式选择仍需结合 SSE 肘部。

### 第二次运行：最终聚类

人工查看三种表示的真实曲线后，同时提供三个最终 k，例如：

```bash
python src/run_basic.py --final-k-raw 4 --final-k-hsv 5 --final-k-hog 5
```

第二阶段将在相同主流程基础上额外输出：

- `results/final_metrics.csv`：最终 k、Silhouette、CH、DB、PCA-50 累计解释方差、k-means 时间和峰值内存增量。
- 各表示的 `pca2_clusters.png`：用 PCA-2D 坐标显示 PCA-50D 聚类得到的簇标签。
- `representatives.png` / `representatives.csv`：在 PCA-50D 聚类空间中距离各簇中心最近的 3 个样本。
- `cluster_composition.csv`：各 cluster 的真实类别组成，仅作聚类后解释。

## 实现与验证状态

- `src/run_basic.py` 已做 Python 语法编译检查，通过。
- 在独立工具环境中使用随机合成的 32×32 RGB 数据检查了三种特征和核心 PCA / k 扫描函数：Raw Pixel 输出 3072D、HSV Histogram 输出 96D、当前 HoG 参数输出 1764D；PCA-50 / PCA-2 与 k 扫描核心流程可执行。
- 上述仅属于代码级最小验证，不是 CIFAR-10 正式实验结果。
- 尚未在用户实际 Python 环境中验证依赖安装、CIFAR-10 下载速度和完整 3000 样本运行时间。
- 时间目前仅包围最终一次 `KMeans.fit`，不把数据下载、特征构建和 PCA 混入“单次聚类时间”。
- 内存目前通过 `psutil` 轮询当前 Python 进程 RSS，报告 `KMeans.fit` 期间峰值相对拟合前基线的增量；正式实验后再判断该口径是否需要在报告中进一步说明。

## 讨论分析与报告素材（尚未验证）

本节用于保存后续报告可能用到的解释、方案评价和实验假设。这里的内容不能在正式结果产生前写成结论。

### 1. 类别选择思路

选择 `airplane`、`automobile`、`frog`、`cat`、`dog`，不是只挑最容易分开的五类，而是希望同时包含视觉差异相对明显的类别和一组较相似的类别。`cat` 与 `dog` 可作为主要困难对，用于观察不同表示在相似类别上的能力边界。

可验证假设：视觉差异较大的类别可能更容易形成清晰簇；`cat` / `dog` 可能更易混淆。该判断必须由实际聚类结果、簇组成和代表样本验证。

### 2. 三种表示的分析角度

- Raw Pixel：保留完整像素与空间位置信息，但可能对位置、背景和局部变化较敏感，可作为最朴素 baseline。
- HSV Histogram：弱化空间位置，主要描述全局颜色分布，适合观察颜色和背景分布是否形成聚类结构。
- HoG：弱化颜色，主要描述局部边缘、梯度方向与轮廓，适合观察形状信息是否比纯像素或颜色更有利于聚类。

三种表示先独立比较，能够让结果解释更直接；若一开始拼接，方法差异和性能变化更难归因。是否增加特征拼接实验留到主实验完成后再决定。

### 3. PCA 相关分析素材

PCA-50D 用作正式聚类空间，PCA-2D 只负责可视化。每种表示独立拟合 PCA，避免把不同语义和不同维度的特征混在同一个 PCA 中。

当前不使用统一 StandardScaler 的考虑是：Raw Pixel 已统一缩放到 `[0,1]`，HSV Histogram 已做通道内 L1 归一化，HoG 已包含 L2-Hys 局部归一化；额外 z-score 会进一步改变原有方差结构。PCA-50 的累计解释方差比例可作为报告中说明“50 维保留了多少数据变化”的辅助信息，但不用于重新选择 PCA 维数。

### 4. k-means 与 k 选择分析素材

k-means 的目标可理解为最小化簇内平方距离和（SSE / inertia）。随着 k 增大，SSE 通常单调下降，因此不能直接选择 SSE 最小的 k，而应观察肘部位置。

Silhouette 同时考虑簇内紧密程度和簇间分离程度，通常越大越好。最终 k 由 SSE 肘部曲线和 Silhouette-k 曲线综合判断；两者若不一致，应如实说明聚类结构不存在唯一明显的簇数，而不是强行让两个指标一致。

虽然数据来自 5 个真实类别，但无监督聚类不能直接因为真实类别数为 5 就预设 `k=5` 为最佳值。不同表示可以选择不同的最终 k，这本身也可能说明不同表示诱导出的几何结构不同。

### 5. 最终指标解释

- Silhouette：越大通常越好，同时考虑样本的簇内紧密与最近其他簇的分离情况。
- Calinski-Harabasz（CH）：越大通常越好，比较簇间离散程度与簇内离散程度。
- Davies-Bouldin（DB）：越小通常越好，反映各簇内部松散程度与相互接近程度。

三个指标侧重点不同，正式结果若不完全一致，应结合曲线、二维可视化和代表样本解释，而不是强行指定单一指标为绝对结论。

### 6. 可能的主报告输出

主实验完成后，可考虑用一张统一表格汇总三种表示：表示方法、PCA-50 累计解释方差、最终 k、Silhouette、CH、DB、聚类耗时和内存。

可视化对应保存：每种表示的 SSE/Silhouette-k 曲线、PCA-2D 聚类散点图、每簇 3 张代表样本，以及必要时的真实类别组成分析。

## 下一步

1. 在实际项目环境安装 `requirements.txt`。
2. 执行第一次 `python src/run_basic.py`，生成三种表示的真实 `k_scan.csv` 与 `k_scan.png`。
3. 读取真实扫描结果，分别判断 Raw Pixel、HSV Histogram、HoG 的最终 k。
4. 带三个 `--final-k-*` 参数执行第二阶段，生成最终指标与可视化。
5. 检查实际结果是否暴露实现问题，再决定是否需要调整时间/内存缩放实验；Basic 完成后才进入 Plus。

## 待确认问题

- 三种表示的最终 k：必须等待真实扫描曲线。
- 主实验完成后是否增加三种特征拼接作为额外对照。
- 时间与内存缩放是否采用 2 个还是 3 个样本量点；该部分可在 3000 样本主结果稳定后再定。
- Plus 三个方向的具体实现方案与参数，等待 Basic 完成后讨论。
