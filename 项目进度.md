# 项目进度

## 新对话恢复提示词

```text
请继续协助我完成“基于 CIFAR-10 子集的表示构建与聚类分析”课堂作业。

以当前打开的 Git 仓库为项目根目录，不假定固定盘符或用户名。开始前依次阅读：
1. AGENTS.md：项目范围、长期协作规则、已确认方案与实施策略。
2. 项目进度.md：当前阶段、讨论分析、实际结果和下一步。
3. README.md，以及当前任务指向的原始要求、代码或结果。

依据实验要求.pdf 和用户已确认要求推进；区分原文要求、已确认方案、未验证设想和实际结果。核对实际文件与必要的 Git 状态，不因对话摘要不完整而重做已经完成的工作。

遵循最小充分方案，只完成当前明确任务，不额外建立哈希、审计、门禁、备份体系或测试框架。交付文件的必要备份按 AGENTS.md 执行，正式文件名保持稳定。

用适合初学者的中文说明目的、输入输出和关键概念；正式实验前说明比较因素、评价方法和结论边界。已授权且明确的工作直接继续，关键要求缺失时再询问。

AGENTS.md 只保存稳定的方案与实施策略；方法分析、方案评价、未验证假设、报告讨论素材、阶段结果和下一步放在本文件。

任务结束或出现重要结果后更新本文件，记录结果路径、必要验证、明确问题和下一步；不把计划或仅本地提交写成已经完成的实验或远程推送。
```

> 更新时间：2026-09-13

## 当前阶段

Basic 方案讨论已基本闭环，已经进入第一版代码实现阶段。`src/run_basic.py` 已建立，覆盖数据抽样、三种表示、PCA、k-means 的 k 扫描、最终指标、二维可视化、代表样本和真实类别组成辅助分析。

3000 样本的首次扫描及最终聚类均已完成，三种表示最终 k 均选 2，真实结果位于 `results/n3000/`。已补充 k=2～12 的二维对比图与逐簇类别统计，并完成 1000/2000/3000 嵌套规模实验；时间有单次观测结果，RSS 增量不足以支持实际内存增长趋势。Plus 尚未开始。

## 已确认选择

- 本项目定位为课堂作业，采用最小充分方案。
- 仓库：<https://github.com/mengmcy1/cifar10-clustering>，私有仓库，默认分支 `main`。
- `AGENTS.md` 只保存长期规则、固定方案和实施策略；本文件保存实时进度、分析评价、报告素材、未验证假设与结果。
- 数据使用 CIFAR-10 的 `airplane`、`automobile`、`frog`、`cat`、`dog` 五类，每类 600 张，共 3000 张；三种表示使用完全相同的样本。
- 第一版实现从 CIFAR-10 train split 中按 `random_state=42` 抽样，并导出 `results/sample_list.csv`。
- 主实验分别使用 Raw Pixel、HSV Color Histogram、HoG 三种表示，不在主实验开始时拼接。
- 三种表示分别独立做 PCA-50D 用于聚类、PCA-2D 用于可视化，不统一追加 `StandardScaler`，不使用 PCA whitening。
- k-means 扫描 `k=2~12`，固定 `init='k-means++'`、`n_init=20`、`random_state=42`。
- 最终 k 必须结合 SSE 肘部和 Silhouette-k 曲线判断，不因为真实类别数为 5 而直接固定 `k=5`。
- 真实类别标签不参与特征构建、PCA 或聚类，只用于抽取指定类别以及聚类后的解释和辅助分析。
- Plus 阶段延后到 Basic 全部完成后；届时三个 Plus 方向都可以尝试，再根据真实结果决定报告重点。

## 已完成

- 创建并初始化 `AGENTS.md`、`README.md`、`项目进度.md`、`.gitignore`，保留原始 `实验要求.pdf`。
- 完整阅读 `实验要求.pdf` 两页内容，确认 Basic 主线包括：5 类 CIFAR-10 子集、表示构建、PCA-50D / PCA-2D、k-means `k=2~12`、SSE 与 Silhouette 选 k、二维可视化、每簇 3 张代表样本、Silhouette / CH / DB、运行时间与内存；Plus 三选一为最低要求。
- 确定 Raw Pixel：展平 3072D，像素缩放到 `[0,1]`。
- 确定 HSV Histogram：H/S/V 各 32 bins，共 96D；各通道分别 L1 归一化。
- 确定 HoG：灰度图，`orientations=9`、`pixels_per_cell=(4,4)`、`cells_per_block=(2,2)`、`block_norm='L2-Hys'`。
- 确定 PCA：每种表示独立拟合 50D 和 2D；不额外 `StandardScaler`；`whiten=False`；记录 PCA-50 累计解释方差比例。
- 确定 k-means 基本参数和 k 扫描策略。
- 新增 `requirements.txt`，包含当前 Basic 所需的 NumPy、pandas、Matplotlib、scikit-learn、scikit-image、psutil、PyTorch / torchvision。
- 新增 `src/run_basic.py`，实现第一版 Basic 主流程。
- 更新 `README.md`，补充环境安装、首次 k 扫描和最终聚类两阶段运行方式。

## 第一版代码行为

### 第一次运行：k 扫描

执行：

```bash
python src/run_basic.py
```

脚本将：

1. 自动下载/读取 CIFAR-10 train split。
2. 固定随机种子，从五个类别各抽 600 张，并输出 `results/sample_list.csv`。
3. 对完全相同的 3000 张图分别构建 Raw Pixel、HSV Histogram、HoG。
4. 每种表示分别拟合 PCA-50D 与 PCA-2D，并记录 PCA-50 累计解释方差。
5. 在 PCA-50D 上扫描 `k=2~12`，保存每个 k 的 SSE 与 Silhouette。
6. 输出每种表示的 `k_scan.csv` 与 `k_scan.png`。
7. 控制台只给出 Silhouette 单指标最高的 k 作为参考，不自动把它写成最终 k；正式选择仍需结合 SSE 肘部。

### 第二次运行：最终聚类

人工查看三种表示的真实曲线后，同时提供三个最终 k，例如：

```bash
python src/run_basic.py --final-k-raw 4 --final-k-hsv 5 --final-k-hog 5
```

第二阶段将在相同主流程基础上额外输出：

- `results/final_metrics.csv`：最终 k、Silhouette、CH、DB、PCA-50 累计解释方差、k-means 时间和峰值内存增量。
- 各表示的 `pca2_clusters.png`：用 PCA-2D 坐标显示 PCA-50D 聚类得到的簇标签。
- `representatives.png` / `representatives.csv`：在 PCA-50D 聚类空间中距离各簇中心最近的 3 个样本。
- `cluster_composition.csv`：各 cluster 的真实类别组成，仅作聚类后解释。

## 实现与验证状态

- `src/run_basic.py` 已做 Python 语法编译检查，通过。
- 在独立工具环境中使用随机合成的 32×32 RGB 数据检查了三种特征和核心 PCA / k 扫描函数：Raw Pixel 输出 3072D、HSV Histogram 输出 96D、当前 HoG 参数输出 1764D；PCA-50 / PCA-2 与 k 扫描核心流程可执行。
- 上述仅属于代码级最小验证，不是 CIFAR-10 正式实验结果。
- 尚未在用户实际 Python 环境中验证依赖安装、CIFAR-10 下载速度和完整 3000 样本运行时间。
- 时间目前仅包围最终一次 `KMeans.fit`，不把数据下载、特征构建和 PCA 混入“单次聚类时间”。
- 内存目前通过 `psutil` 轮询当前 Python 进程 RSS，报告 `KMeans.fit` 期间峰值相对拟合前基线的增量；正式实验后再判断该口径是否需要在报告中进一步说明。

## 讨论分析与报告素材（尚未验证）

本节用于保存后续报告可能用到的解释、方案评价和实验假设。这里的内容不能在正式结果产生前写成结论。

### 1. 类别选择思路

选择 `airplane`、`automobile`、`frog`、`cat`、`dog`，不是只挑最容易分开的五类，而是希望同时包含视觉差异相对明显的类别和一组较相似的类别。`cat` 与 `dog` 可作为主要困难对，用于观察不同表示在相似类别上的能力边界。

可验证假设：视觉差异较大的类别可能更容易形成清晰簇；`cat` / `dog` 可能更易混淆。该判断必须由实际聚类结果、簇组成和代表样本验证。

### 2. 三种表示的分析角度

- Raw Pixel：保留完整像素与空间位置信息，但可能对位置、背景和局部变化较敏感，可作为最朴素 baseline。
- HSV Histogram：弱化空间位置，主要描述全局颜色分布，适合观察颜色和背景分布是否形成聚类结构。
- HoG：弱化颜色，主要描述局部边缘、梯度方向与轮廓，适合观察形状信息是否比纯像素或颜色更有利于聚类。

三种表示先独立比较，能够让结果解释更直接；若一开始拼接，方法差异和性能变化更难归因。是否增加特征拼接实验留到主实验完成后再决定。

### 3. PCA 相关分析素材

PCA-50D 用作正式聚类空间，PCA-2D 只负责可视化。每种表示独立拟合 PCA，避免把不同语义和不同维度的特征混在同一个 PCA 中。

当前不使用统一 StandardScaler 的考虑是：Raw Pixel 已统一缩放到 `[0,1]`，HSV Histogram 已做通道内 L1 归一化，HoG 已包含 L2-Hys 局部归一化；额外 z-score 会进一步改变原有方差结构。PCA-50 的累计解释方差比例可作为报告中说明“50 维保留了多少数据变化”的辅助信息，但不用于重新选择 PCA 维数。

### 4. k-means 与 k 选择分析素材

k-means 的目标可理解为最小化簇内平方距离和（SSE / inertia）。随着 k 增大，SSE 通常单调下降，因此不能直接选择 SSE 最小的 k，而应观察肘部位置。

Silhouette 同时考虑簇内紧密程度和簇间分离程度，通常越大越好。最终 k 由 SSE 肘部曲线和 Silhouette-k 曲线综合判断；两者若不一致，应如实说明聚类结构不存在唯一明显的簇数，而不是强行让两个指标一致。

虽然数据来自 5 个真实类别，但无监督聚类不能直接因为真实类别数为 5 就预设 `k=5` 为最佳值。不同表示可以选择不同的最终 k，这本身也可能说明不同表示诱导出的几何结构不同。

### 5. 最终指标解释

- Silhouette：越大通常越好，同时考虑样本的簇内紧密与最近其他簇的分离情况。
- Calinski-Harabasz（CH）：越大通常越好，比较簇间离散程度与簇内离散程度。
- Davies-Bouldin（DB）：越小通常越好，反映各簇内部松散程度与相互接近程度。

三个指标侧重点不同，正式结果若不完全一致，应结合曲线、二维可视化和代表样本解释，而不是强行指定单一指标为绝对结论。

### 6. 可能的主报告输出

主实验完成后，可考虑用一张统一表格汇总三种表示：表示方法、PCA-50 累计解释方差、最终 k、Silhouette、CH、DB、聚类耗时和内存。

可视化对应保存：每种表示的 SSE/Silhouette-k 曲线、PCA-2D 聚类散点图、每簇 3 张代表样本，以及必要时的真实类别组成分析。

## 下一步

### 2026-09-13 基础代码审阅

- 用户再次明确：先完成 Basic，再详细安排 Plus，本轮仅检查基础代码并说明实现内容。
- 已阅读 `src/run_basic.py`、依赖清单及输出逻辑；主计算流程与当前已确认方案一致，本轮未修改源码、未运行正式实验。
- 当前默认 Python 为 3.14.5，可发现 NumPy、pandas；缺少 Matplotlib、scikit-learn、scikit-image、psutil、torch、torchvision，尚不能在该解释器中运行完整脚本。此检查不代表其他 Python 环境也缺少依赖。
- 本轮验证：内存中语法编译通过；直接提取源码中的 Raw Pixel 与代表样本函数，使用 NumPy 小数组验证展平、缩放、簇内最近样本排序，均通过；未验证其余依赖库的实际运行。
- 已发现：重复运行会直接覆盖同名 CSV/图像，脚本没有执行交付文件备份；更换样本量仍使用同一输出目录时也会覆盖不同条件的结果，重新只扫描还可能留下旧的最终结果文件。
- 内存字段是拟合期间进程 RSS 峰值相对拟合前的增量，并非总内存；前序扫描后的内存复用及 10 ms 采样可能使其偏小，不宜据此直接解释算法总内存或规模趋势。计时实际包括模型构造与 `fit`，比此前“仅包围 fit”的描述略宽。
- 小簇不足 3 张时函数只返回现有样本，图中其余位置为空且无提示；已用单样本簇复现，不能默认每簇都能展示 3 张。
- 最终阶段仍会重做全部 k 扫描，属于重复计算；样本量参数已有，但多规模结果汇总及分析尚未完成。

### 2026-09-13 作业要求与拆解复核

- 重新完整核对 `实验要求.pdf`（两页，文字为矢量轮廓，经 poppler 渲染读取）与《人工智能I_实验报告要求.docx》，确认 AGENTS.md 第 10 节的 Basic 拆解与作业原文逐条对应，整体合理，本轮未修改代码与方案。
- 注意点 1：PDF 任务描述中写有"并与至少一种替代方法（GMM/DBSCAN/谱聚类）进行对比"；Plus 为"选一"，其中"替代聚类"恰好覆盖该句。为稳妥起见，Plus 三个方向中应优先保证"替代聚类"进入正式报告。
- 注意点 2：报告要求 docx 的 5.3 节要求"选择至少 1 个重要参数修改并重新运行至少 3 次"，5.4 节要求对比图；现有 `k=2~12` 扫描（11 个取值、Silhouette-k 曲线）天然满足，报告写作时按"参数修改实验"口径组织 k 扫描结果即可，不需额外实验。
- 注意点 3：报告要求需要 1 张终端环境截图（体现 Python 版本、依赖版本、运行目录）和不少于 2 张实际运行过程截图；正式运行时需同步截图留存，事后难以补拍同等证据。
- 注意点 4：作业原文"原始像素标准化"按已确认方案实现为 `[0,1]` 缩放（归一化），报告中需说明该口径及理由，避免与 z-score 标准化混淆。
- 注意点 5：任务描述要求"给出对参数与数据规模的敏感性结论"；参数敏感性由 k 扫描覆盖，数据规模敏感性由 2~3 个样本量点的时间/内存缩放覆盖，报告需明确写出这两类结论。

### 2026-09-13 四条已知问题的处理决定

- 已确认：同条件重跑覆盖已有交付图表前，仍需按 AGENTS.md §5.2 备份一次到 `历史版本/` 对应子目录；"固定文件名、结果可再生成"不免除该义务。不同实验条件通过 `--results-dir` 区分结果目录，不额外增加旧结果警告等机制。
- 已确认：内存字段保留现有实现，但口径仅为"拟合期间观测到的进程 RSS 峰值相对基线的增量"，受前序计算后的内存复用及 10 ms 采样影响，不等同于算法实际内存需求；若实测接近零，不得据此解释内存随样本量的变化。报告需写明该口径，正式结果出来后再判断是否足以支持分析。
- 已确认："小簇不足 3 张概率低""重复扫描几分钟量级"均为未验证预估，不是已确认依据。正式运行后检查各簇样本量与实际耗时，再决定是否处理空白子图。
- 重复扫描暂不优化；样本量缩放（2 个还是 3 个样本量点）继续保留为计划内事项。

### 2026-09-13 第一次 k 扫描结果（n3000）

- 首次正式扫描完成，结果位于 `results/n3000/`；样本清单核验通过，指定五类各 600 张，共 3000 张，无重复数据集索引。
- 本次三种表示的 Silhouette 均在 k=2 时最高：Raw 0.2110、HSV 0.3600、HoG 0.0925。该结果仅适用于当前样本与实验设置，尚未验证稳定性或二分的语义。
- Raw 的 SSE 在 k≈4 附近有一定转折；HSV、HoG 的转折较平缓。k=4 可作为细分候选，但现有证据不足以认定三者均具有明确的 k=4 肘部。
- 后续已确认三种表示最终 k 均取 2，并完成第二阶段；不将此选择解释为唯一真实簇数。

### 2026-09-13 全 k 二维可视化与类别统计

- 用户要求补充三种表示 k=2～12 每种划分的二维图和统计，本轮由助手实现并运行；不改变主方案 k=2。
- 新增 `src/visualize_k_scan.py`，复用 Basic 的抽样、特征、PCA 和 k-means 实现。在 `general`（Python 3.13.14）环境运行，随机种子 42、每类 600 张、n_init=20，重新拟合 33 种划分；标签仅用于聚类后统计。
- 每种表示新增 `pca2_all_k.png`（11 个 k 子图，固定二维坐标和轴范围）及 `all_k_cluster_composition.csv`（k、簇编号、样本数、样本占比、五类数量）。不同 k 的簇编号和颜色不表示样本集合相同。原有扫描和最终结果文件未覆盖。
- 验证通过：样本清单与原实验完全一致；33 次拟合的 SSE 均与原扫描在相对容差 1e-5 内一致；每份统计表 77 行，每个 k 总数 3000、每类 600，各簇类别数量之和等于簇大小、占比之和为 1；k=2 组成与已有最终结果一致；三张图已目视检查。
- 全 k 范围内最小簇大小：Raw 156、HSV 60、HoG 160，未出现不足 3 张的簇。运行出现 torchvision 读取数据时的 NumPy 弃用警告，但流程完成；未修改第三方库。

### 后续执行顺序

1. 最终聚类及全 k 逐簇统计的详细解读已完成；按用户明确指定，报告素材保存于 [AGENTS.md 第 11 节](AGENTS.md#11-n3000-实验报告分析素材)，含数据入口、比例口径、三种表示的逐簇例证、PCA/指标解释及结论边界。此为文档分工的指定例外，实时进度仍在本文件。
2. 嵌套规模实验已执行并验证，结果与限制见下节；整理报告时保留单次计时和 RSS 增量口径限制。
3. Basic 完成后再详细安排 Plus。

## 待确认问题

- 三种表示的最终 k 已确定为 2，不再待定。
- 主实验完成后是否增加三种特征拼接作为额外对照。
- 规模实验已采用三个嵌套样本量点，不再待定；现有 RSS 测量不支持实际内存增长趋势。
- Plus 三个方向的具体实现方案与参数，等待 Basic 完成后讨论。

## 2026-09-13 嵌套规模实验结果

详细报告素材已补入 [AGENTS.md 第 11.10 节](AGENTS.md#1110-嵌套样本量实验设计结果与分析)，含设计依据、耗时与指标对照、内存测量限制、与主实验的可比性及可引用总结。本节保留运行记录和结果入口。

本轮获用户授权后由助手实现并运行。新增 `src/run_scaling.py`，复用 Basic 的三种表示和资源测量函数；`run_basic.py` 仅将计时起点移到模型构造之后，严格测量 fit。母集为 `results/n3000/sample_list.csv`，按清单顺序每类取前 200/400/600 张，不重新随机抽样，三种表示共享各规模的样本；每个规模单独启动 Python 进程，顺序为 1000、2000、3000，各进程内依次执行 Raw、HSV、HoG。每个规模独立拟合 PCA-50，种子 42，whiten=False，k=2，n_init=20。未运行 GPU 计算。

运行环境：Windows、Conda general、Python 3.13.14，Intel Core Ultra 7 255HX（系统报告 20 核/20 逻辑处理器）；NumPy 2.4.4、pandas 3.0.5、scikit-learn 1.9.1、scikit-image 0.26.0、psutil 7.1.0、torch 2.8.0+cu128、torchvision 0.23.0+cu128。未人为修改线程数。torchvision 数据读取出现已有 NumPy 弃用警告，运行成功，未修改第三方库。

每个 `results/n1000/`、`results/n2000/`、`results/n3000/` 新增 `scaling_sample_list.csv`、`scaling_metrics.csv`。本轮没有覆盖既有交付文件，因此无需覆盖备份；原 `final_metrics.csv` 保留原先扫描后测量结果，不能与本轮资源列混用。新表记录 n_iter，但它只对应最终保留解，不是 20 次初始化的总迭代数。

| 表示 | N | Silhouette | CH | DB | fit 时间（秒） | RSS 峰值增量（MiB） |
|---|---:|---:|---:|---:|---:|---:|
| Raw | 1000 | 0.1992 | 293.98 | 1.7718 | 0.1591 | 8.5391 |
| Raw | 2000 | 0.2064 | 620.62 | 1.7248 | 0.1917 | 5.3125 |
| Raw | 3000 | 0.2110 | 948.94 | 1.7025 | 0.2349 | 19.4648 |
| HSV | 1000 | 0.3189 | 148.19 | 1.3243 | 0.1039 | 1.7109 |
| HSV | 2000 | 0.3445 | 304.88 | 1.2382 | 0.1350 | 1.3086 |
| HSV | 3000 | 0.3600 | 539.30 | 1.1898 | 0.1740 | 0.2344 |
| HoG | 1000 | 0.0852 | 89.37 | 3.2295 | 0.1069 | 0.0508 |
| HoG | 2000 | 0.0914 | 193.44 | 3.0871 | 0.1280 | 0.1055 |
| HoG | 3000 | 0.0925 | 295.15 | 3.0662 | 0.1643 | 0.0938 |

验证：三份清单与母集逐类前缀完全一致，1000⊂2000⊂3000，3000 的行顺序与原母集一致；每个规模有三种表示的有效指标，簇大小合计等于 N；3000 的 Silhouette、CH、DB、PCA-50 解释方差与原主结果一致（相对容差 1e-7）。运行已覆盖真实关键路径，不另建测试框架。

分析与报告边界：

- 三种表示的本轮单次 fit 耗时均随 N 增加，但仅三个规模、每条件一次，不能据此证明线性复杂度或稳定的效率排名。计时不含数据准备、特征、PCA、指标和绘图，也不代表总流水线耗时。
- Silhouette 随 N 增大略有提高、DB 略有降低，三种表示的这两项排序未改变；这是本次嵌套样本和重新拟合 PCA 下的观测，不能归因于纯粹增加 N。CH 含样本数相关项，不应将其随 N 增大直接解释为同比例质量改善；不同 N 的原始 SSE 也不宜直接比较好坏。
- RSS 增量明显不单调：Raw 先降后升，HSV 持续下降，HoG 接近零。各规模独立进程并未消除进程内部前序特征、PCA、其他表示计算及分配器复用的影响。该口径不足以推断算法实际内存随 N 的变化，不拟合或强行绘制增长趋势，不据此声称样本更多反而更省内存。
- 3000 的新旧聚类质量一致而资源数值不同，符合两次测量前序计算条件不同；本轮未先做 k 扫描。报告应使用本轮三个规模的资源记录进行受限比较，旧记录作为原主实验测量保留。
- 本轮完成规模实验执行与结果解释；是否进一步改善内存测量由用户根据报告需要决定，当前不扩大为新 benchmark 系统。下一步可先审阅本节，确认 Basic 素材后讨论 Plus。
