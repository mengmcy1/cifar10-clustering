# 项目协作说明

## 1. 项目目标与范围

项目名称：基于 CIFAR-10 子集的表示构建与聚类分析。

本项目是一份课堂作业，以根目录 `实验要求.pdf` 和用户明确确认的要求为依据，完成所需的实现、实验分析和报告。

- 优先满足作业要求，保证代码能够运行、结果真实、分析清楚。
- 不主动扩展为科研平台、通用框架或生产系统。
- 未核实的算法、指标、数据规模和交付要求不得写成既定事实。
- 区分作业原文要求、已经确认的实现方案和实际实验结论。
- 可选拓展先简短说明价值，未经用户要求不直接实施。

## 2. 最小充分方案与反过度工程

以**最小充分方案（Minimum Sufficient Solution）**完成当前明确任务。

优先级：

1. 正确完成当前任务。
2. 修改范围尽可能小。
3. 复用现有代码、结构和项目机制。
4. 进行足以证明修改有效的最小验证。
5. 不主动扩大任务范围。

每准备增加一个步骤、文件、抽象或机制前，先判断：

> 如果省略它，是否仍能可靠完成当前明确任务？

如果可以，默认省略。不要把“可能有用”误判成“当前必须执行”。

默认禁止主动新增：

- 哈希校验、唯一基线管理、协议冻结和门禁体系。
- 额外备份、恢复流程和回滚演练（第 5 节用户明确要求的交付文件备份除外）。
- 专门的 smoke test 流程、全量 dry-run、验收矩阵。
- CI/CD、审计链路、版本冻结机制和新的测试框架。
- 一次性辅助脚本、工具类、管理器或无必要的抽象层。

只有用户明确要求、这些机制是完成当前任务所必需的，或能够指出具体、现实的失败场景并解释现有机制为何不足时，才允许新增。不得删除、绕过或弱化已有安全措施。

正常的科研 baseline、作业要求的对照方法和实验比较应正常完成，不将其扩展为额外的基线管理或审计体系。

## 3. 实现与验证原则

- 先阅读直接相关的代码和要求，再确定最小修改点。
- 修复问题采用“定位 → 最小修改 → 复现验证”的方式。
- 运行实验以执行目标流程、检查关键输出为目标。
- 不提前设计扩展点，不为未来可能的需求增加功能。
- 信任内部代码、类型系统和框架已经明确提供的保证。
- 只在用户输入、外部 API、网络和文件格式等真实边界进行必要校验。
- 不吞掉错误，不使用无明确必要性的宽泛异常捕获、静默默认值或回退逻辑。

验证只覆盖本次修改及必要的直接调用路径，优先复用已有测试。除非改动确实影响广泛或用户明确要求，不自动执行全项目测试或全量回归。高成本训练前可以进行一次必要的最小运行检查。

## 4. 实验与结果原则

每轮正式实验前，简要说明：

1. 本轮解决什么问题。
2. 使用什么输入、产生什么输出。
3. 主要比较哪些因素。
4. 如何评价，结果能支持什么结论。
5. 完成后据此决定什么。

计划明确且已经授权时，说明后直接继续。正式实验记录应包含必要的数据及划分、对照方法、关键参数、评价指标、输出位置和结论边界；检验假设时说明什么结果会否定假设，不为简单作业步骤强设研究流程。

- 使用到随机抽样或随机算法时，记录随机种子及影响结果的关键参数。
- 比较方法时保持必要的数据与评价条件一致；条件不一致时明确说明。
- 消融实验尽量一次改变一个因素；完整方法比较存在多项差异时，不作单一因素归因。
- 如使用真实类别标签，应说明用途，区分标签参与表示构建与仅用于结果评价的情况。
- 文献结果、官方代码复现和本项目结果分别注明来源，不混用评价口径。
- 工具运行成功不等于方法有效，相对改善不等于绝对可用。
- 不编造数据、指标或运行记录，不把计划写成已完成事项。
- 保留有分析价值的失败结果，说明结论适用范围。
- 只记录复现和解释当前结果所需的信息，不自行建设实验管理系统。

涉及文献与方法探索时，采用“阅读提出问题 → 证据形成假设 → 实验验证 → 失败案例界定边界 → 汇报形成结论”的工作方式。

使用 NVIDIA GPU 进行训练、推理或批量生成前，必须检查 `nvidia-smi` 的显存、利用率和进程，在已分配或获准使用的 GPU 中显式选择设备，不能仅凭空闲判断使用权限。不得结束其他用户或来源不明的进程。

## 5. 文件组织与文档分工

以当前打开的 Git 仓库为项目根目录，不依赖固定盘符或用户名。

| 文件或目录 | 职责 |
|---|---|
| `README.md` | 项目简介、文件导航、实际运行方式 |
| `AGENTS.md` | 项目范围、长期协作规则、原则及用户明确要求固化的实验设计基线 |
| `项目进度.md` | 当前状态、关键结果、问题和下一步 |
| `实验要求.pdf` | 原始作业要求 |
| `src/` | 正式实现代码 |
| `results/` | 必要的结果表和图 |
| `report/` | 作业报告及交付材料 |
| `data/` | 本地数据，默认不提交原始数据集 |
| `历史版本/` | 覆盖更新前的交付文件备份，默认仅本地保留 |

目录在实际需要时建立，不为凑齐结构创建空目录或占位文件。正式运行代码不能放入临时目录，项目文件不散落到桌面或其他无关位置。

### 5.1 文档维护与项目接续

- `AGENTS.md` 主要记录长期规则、项目范围和正式目录约定；当用户明确要求把已确认的实验设计作为后续协作基线固化时，也可记录，但不得把单轮运行结果或临时设想写入其中。
- `项目进度.md` 是唯一实时进度入口，开头保留新对话恢复提示词，明确阅读顺序和如何继续当前任务。
- 开始任务时，按需阅读进度及其指向的文件，确认当前阶段、已确认选择和下一步，不因对话上下文缺失从头重做。
- 当前阶段、启动条件、待确认问题和阶段结果主要在进度文档维护；已由用户明确要求固化的长期实验设计可在 `AGENTS.md` 保留一份基线说明。
- 进度文档记录更新时间、当前阶段、已完成事项、关键结果及路径、明确问题和下一步。
- 在任务完成、重要结果产生或安排改变后更新进度，不为每条命令追加记录。
- 历史内容较多时再按需整理，当前进度保持简洁，不预建复杂归档体系。
- 规则以 `AGENTS.md` 为准；任务完成情况以实际代码、输出及验证记录为依据，发现进度描述不符时及时纠正。
- 用户已明确要求创建或修改的文档可直接处理；用户要求先审阅时，先提供草稿，确认后再写入。

### 5.2 文件迭代与备份

同一交付文件持续修改时，保持正式路径和文件名稳定。不得因修改而不断生成 `_简洁版`、`_修改版`、`_最终版` 等并行文件。

对已有报告、PPT、图表等交付文件进行覆盖更新前，先将旧文件备份到项目根目录 `历史版本/` 下对应的文件子目录，备份文件名附加日期时间；随后将新内容写回原正式路径。备份目录默认仅本地保留，由 `.gitignore` 排除。

一次连续修改任务在首次覆盖前备份一次，不为每次保存或内部调整重复备份。不同实验条件产生的独立结果应按实验含义命名，不互相覆盖。

源码、配置、`README.md`、`AGENTS.md` 和 `项目进度.md` 的日常修改由 Git 管理历史，不逐次建立文件副本。用户明确要求备份某个文件时，按要求执行。

## 6. 沟通与阶段讲解

- 默认使用中文，用户正在逐步建立表示学习、降维与聚类知识，不直接堆叠术语。
- 术语首次出现时，按“通俗解释 → 中文专业名称 → 英文全称 → 缩写 → 在本项目中的作用”介绍；没有必要时不强行补齐英文和缩写。
- 解释方法时说明输入、输出、核心思路、解决的问题、局限及与作业的关系。
- 推荐论文或代码时说明重要程度、阅读难度、重点内容、开源状态和使用成本，帮助理解方法之间的联系。
- 阶段复盘按照“目标 → 完成工作 → 结果证据 → 结论与限制 → 下一步及成功标准”说明。
- 区分已验证事实、根据证据作出的判断、未验证设想和计划，不将计划中的创新描述为已实现成果。
- 用户要求分步骤教学时，每次只讲一个主题，允许暂停、追问或调整深度。
- 对已授权的工作持续推进，不重复请求确认；关键要求缺失时再询问。
- 完成后简洁说明做了什么、如何验证、验证结果及仍存在的明确问题。
- 长任务避免高频无意义轮询，在完成、出错、需要输入或出现实质结果时处理。

## 7. 论文阅读与总结

阅读论文前先介绍问题、方法和输入输出，按实际贡献说明创新点及对比方法，指出重点章节、阅读优先级、与作业的关系和潜在局限，不为凑数量罗列创新点。

优先阅读摘要、方法总览、表示构建、降维或聚类方法、关键设计、实验、消融和失败案例等与当前问题直接相关的内容。

逐段精读采用：

1. 原意翻译：忠实表达原意。
2. 大意梳理：说明该段在全文中的作用。
3. 概念拆解：用直觉、流程或例子解释关键概念。

用户可以直接跳到代码或具体问题。总结覆盖动机、方法流程、关键概念、创新点、核心设计、实验、局限和可借鉴内容；尚未读完时先简记到进度或明确的临时位置，不自动为每篇论文建立重复笔记体系。新的总结文档按第 5.1 节处理，用户已指定位置和内容时不重复申请审批。

## 8. 文件安全与运行环境

- 禁止批量或递归删除文件和目录，不使用 `del /s`、`rd /s`、`rmdir /s`、`Remove-Item -Recurse`、`rm -rf` 等命令。
- 删除前核实目标，每次只能删除一个明确绝对路径的普通文件。
- 不使用通配符或未解析的环境变量、命令替换确定删除目标。
- PowerShell 删除使用 `Remove-Item -LiteralPath` 指定明确绝对路径。
- 需要批量删除或删除目录时，停止并要求用户手动处理。
- 不擅自覆盖原始材料、重要结果或用户已有修改。

Windows 默认使用 PowerShell 7（`pwsh.exe`）。首次执行终端命令前检查 `$PSVersionTable.PSVersion`，主版本必须不低于 7；不可用时报告问题，不回退到 PowerShell 5.1。

## 9. Git 与 GitHub

仓库地址：`https://github.com/mengmcy1/cifar10-clustering`

- 用户明确要求“提交”时，执行 `commit + push`，无需重复确认。
- 提交前查看变更范围，确保只包含当前任务相关内容。
- 提交代码、必要配置、项目文档和适合入库的结果。
- 原始数据集、虚拟环境、缓存、模型权重、大型中间产物和交付文件备份默认通过 `.gitignore` 排除。
- 不提交密码、令牌、私钥或包含认证信息的远程地址。
- 使用已有 GitHub 认证；认证失败时明确报告，不输出或保存令牌。
- 未经明确授权，不强制推送或重写 Git 历史。
- 推送后检查远程同步状态，失败时如实记录，不将本地提交等同于已推送。

## 10. 用户已确认的当前实验设计基线

本节记录用户在讨论中已经明确认可、并要求写入 `AGENTS.md` 作为后续协作基线的实验思路。它们属于**已确认实现方案**，不是 `实验要求.pdf` 的逐字原文，也不是已经通过实验得到的结论。用户后续若明确修改其中任一项，以最新确认内容为准；具体运行结果、阶段状态和待办仍写入 `项目进度.md`。

### 10.1 数据子集与分析目的

- 从 CIFAR-10 中选择 5 个类别：`airplane`、`automobile`、`frog`、`cat`、`dog`。
- 类别选择不是单纯追求容易聚类，而是有意识地同时包含视觉差异较大的类别和一组较相似的类别；其中 `cat` 与 `dog` 作为主要困难对，用于观察不同表示方法在相似类别上的能力边界。
- 按原始作业要求，每类样本数不超过 600；当前默认方案为每类取 600 张，共 3000 张，在数据正常可用的前提下执行。
- 抽样使用固定随机种子，并按作业要求导出样本清单 CSV。随机种子的具体数值在正式实现时统一确定，不为不同表示单独抽样。
- 三种表示方法必须使用完全相同的样本集合，避免数据差异影响方法比较。
- CIFAR-10 真实类别标签不得参与特征构建、PCA 拟合或 k-means 聚类；标签仅在聚类完成后用于解释结果、分析各簇真实类别组成等事后评价。
- “差异较大的类别更容易形成清晰结构”“cat 与 dog 更容易混淆”等当前都属于实验假设，必须由实际结果验证，不能提前写成实验结论。

### 10.2 三种表示均独立完成

本项目不只选择一种表示，而是对 Raw Pixel、颜色直方图和 HoG 三种表示分别完成同一套 PCA 与聚类流程。三种特征首先独立比较，不在主实验一开始就拼接，以便能够判断不同信息来源对聚类结果的影响。

#### Raw Pixel

- CIFAR-10 单张 RGB 图像尺寸为 `32×32×3`。
- 直接展平为 3072 维向量。
- 像素值由 `0~255` 缩放到 `0~1`。
- 不额外加入图像增强、滤波或复杂预处理。
- 后续单独拟合 PCA，并降到 50 维用于聚类。

#### HSV Color Histogram

- RGB 图像先转换到 HSV 颜色空间。
- H、S、V 三个通道分别计算直方图。
- 每个通道使用 32 个 bins，拼接后得到 96 维特征。
- 每个 H、S、V 通道的直方图分别做 L1 归一化，使各 bin 表示该通道中的像素比例，再将三个通道拼接。
- 选择 96 维而不是低于 50 维的直方图设计，是为了能够按作业统一流程继续 PCA 到 50 维。
- 该表示主要用于观察全局颜色分布对聚类结构的贡献。

#### HoG

- HoG 使用灰度图计算，使这一支主要描述边缘、轮廓和局部形状，而不重新混入颜色信息。
- 当前固定参数：`orientations=9`、`pixels_per_cell=(4,4)`、`cells_per_block=(2,2)`、`block_norm='L2-Hys'`。
- 对 `32×32` 图像使用 `4×4` cell，以保留比 `8×8` cell 更细的局部结构，同时计算量仍然很低。
- 按当前参数和常规 HoG 实现，特征维度约为 1764 维；正式代码以实际库输出维度为准，不硬编码假定值。
- 后续单独拟合 PCA，并降到 50 维用于聚类。
- 不为本课堂作业额外扫描多组 HoG 参数，除非后续实际结果暴露出明确问题或用户要求比较。

### 10.3 PCA、聚类与统一比较框架

- Raw Pixel、HSV Histogram、HoG 各自独立拟合 PCA，不把三种特征混在一起拟合一个 PCA。
- 每种表示均直接从各自的原始特征空间分别拟合 `PCA(n_components=50)` 和 `PCA(n_components=2)`；50 维结果用于正式聚类，2 维结果用于可视化，不采用“先降到 50 维再从 50 维降到 2 维”的串联方式。
- Raw Pixel 仅将像素从 `0~255` 缩放到 `[0,1]`，PCA 前不再使用 `StandardScaler`；同一表示内部像素量纲一致，并保留像素位置自身的方差差异。
- HSV Histogram 在三个通道各自完成 L1 归一化后直接进入 PCA，不再使用 `StandardScaler`。
- HoG 依赖 `L2-Hys` 完成局部块归一化，之后直接进入 PCA，不再额外使用 `StandardScaler`。
- 三种表示采用“各自必要的最小归一化 + PCA”的策略，而不是为了形式统一而对所有特征机械套用同一层 z-score 标准化。
- `sklearn.decomposition.PCA` 自身会进行均值中心化，因此不额外手工减均值。
- PCA 使用默认的 `whiten=False`，不对白化后的主成分重新等方差缩放，避免无必要地改变后续 k-means 的欧氏距离结构。
- 记录每种表示 PCA-50 的累计解释方差比例，作为理解 50 维压缩保留信息量的辅助结果；不根据该比例自动改变 PCA 维数，也不额外扫描不同维数，因为作业已经指定 50 维。
- 主聚类实验直接在选出的 3000 张样本上进行特征构建、PCA 和聚类，不额外划分监督学习意义上的 train/test；真实标签在这一流程中不参与拟合。
- k-means 在 `k=2~12` 范围内扫描。
- 使用 SSE 肘部曲线和 Silhouette-k 曲线共同分析合适的 k，不因数据来自 5 个真实类别就直接预设 `k=5` 为最优答案。
- 三种表示的样本、PCA 输出维度、k 扫描范围和评价条件保持一致，确保比较有意义。
- 最终评价至少覆盖作业要求中的 Silhouette、Calinski-Harabasz（CH）和 Davies-Bouldin（DB），并记录单次聚类运行时间与内存。
- 按作业要求展示 PCA-2D 聚类结果，并为每个簇展示 3 张最接近簇中心的代表样本。
- 可进一步查看每个 cluster 的真实类别组成来解释簇为什么形成、哪些类别容易混淆；该分析只使用聚类后的标签信息，不反向影响聚类过程。
- 作业要求允许通过两到三个样本量点观察时间和内存缩放；如实现该部分，保持方案简单，不建立专门 benchmark 系统。

### 10.4 当前比较问题与结论边界

三种表示的主实验希望回答：

1. Raw Pixel、HSV Color Histogram 和 HoG 在同一 CIFAR-10 子集上会形成怎样不同的聚类结构？
2. 颜色信息、像素级信息和形状/边缘信息分别在哪些类别上表现较好或较差？
3. 视觉差异明显的类别和 `cat`/`dog` 这类相似类别是否呈现不同的聚类难度？
4. 聚类内部指标、二维可视化和代表样本所表达的现象是否相互一致？

任何“某种表示一定最好”“HoG 一定比 Raw Pixel 更适合 cat/dog”“HSV 一定能分开某些类别”等判断，目前都只是可能的解释方向。只有实际运行后，才能依据指标、可视化和代表样本给出结论。

### 10.5 尚未确定的实验选择

以下事项尚未完成讨论或确认，不得擅自写成固定方案：

- k-means 的具体实现参数，如 `n_init` 等；随机性应按第 4 节原则记录。
- Plus 三选一的最终选择：替代聚类、轻量自编码器表示或多随机种子稳定性。
- 是否在主实验完成后增加三种特征拼接实验；当前主线只做三种独立表示。
- 任何基于实际运行结果才能确定的最佳 k、最佳表示和最终指标数值。