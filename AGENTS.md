# 项目协作说明

## 1. 项目目标与范围

项目名称：基于 CIFAR-10 子集的表示构建与聚类分析。

本项目是一份课堂作业，以根目录 `实验要求.pdf` 和用户明确确认的要求为依据，完成所需的实现、实验分析和报告。

- 优先满足作业要求，保证代码能够运行、结果真实、分析清楚。
- 不主动扩展为科研平台、通用框架或生产系统。
- 未核实的算法、指标、数据规模和交付要求不得写成既定事实。
- 区分作业原文要求、已经确认的实现方案和实际实验结论。
- 可选拓展先简短说明价值，未经用户要求不直接实施。

## 2. 最小充分方案与反过度工程

以**最小充分方案（Minimum Sufficient Solution）**完成当前明确任务。

优先级：

1. 正确完成当前任务。
2. 修改范围尽可能小。
3. 复用现有代码、结构和项目机制。
4. 进行足以证明修改有效的最小验证。
5. 不主动扩大任务范围。

每准备增加一个步骤、文件、抽象或机制前，先判断：

> 如果省略它，是否仍能可靠完成当前明确任务？

如果可以，默认省略。不要把“可能有用”误判成“当前必须执行”。

默认禁止主动新增：

- 哈希校验、唯一基线管理、协议冻结和门禁体系。
- 额外备份、恢复流程和回滚演练（第 5 节用户明确要求的交付文件备份除外）。
- 专门的 smoke test 流程、全量 dry-run、验收矩阵。
- CI/CD、审计链路、版本冻结机制和新的测试框架。
- 一次性辅助脚本、工具类、管理器或无必要的抽象层。

只有用户明确要求、这些机制是完成当前任务所必需的，或能够指出具体、现实的失败场景并解释现有机制为何不足时，才允许新增。不得删除、绕过或弱化已有安全措施。

正常的科研 baseline、作业要求的对照方法和实验比较应正常完成，不将其扩展为额外的基线管理或审计体系。

## 3. 实现与验证原则

- 先阅读直接相关的代码和要求，再确定最小修改点。
- 修复问题采用“定位 → 最小修改 → 复现验证”的方式。
- 运行实验以执行目标流程、检查关键输出为目标。
- 不提前设计扩展点，不为未来可能的需求增加功能。
- 信任内部代码、类型系统和框架已经明确提供的保证。
- 只在用户输入、外部 API、网络和文件格式等真实边界进行必要校验。
- 不吞掉错误，不使用无明确必要性的宽泛异常捕获、静默默认值或回退逻辑。

验证只覆盖本次修改及必要的直接调用路径，优先复用已有测试。除非改动确实影响广泛或用户明确要求，不自动执行全项目测试或全量回归。高成本训练前可以进行一次必要的最小运行检查。

## 4. 实验与结果原则

每轮正式实验前，简要说明：

1. 本轮解决什么问题。
2. 使用什么输入、产生什么输出。
3. 主要比较哪些因素。
4. 如何评价，结果能支持什么结论。
5. 完成后据此决定什么。

计划明确且已经授权时，说明后直接继续。正式实验记录应包含必要的数据及划分、对照方法、关键参数、评价指标、输出位置和结论边界。

- 使用到随机抽样或随机算法时，记录随机种子及影响结果的关键参数。
- 比较方法时保持必要的数据与评价条件一致；条件不一致时明确说明。
- 消融实验尽量一次改变一个因素；完整方法比较存在多项差异时，不作单一因素归因。
- 如使用真实类别标签，应说明用途，区分标签参与表示构建与仅用于结果评价的情况。
- 文献结果、官方代码复现和本项目结果分别注明来源，不混用评价口径。
- 工具运行成功不等于方法有效，相对改善不等于绝对可用。
- 不编造数据、指标或运行记录，不把计划写成已完成事项。
- 保留有分析价值的失败结果，说明结论适用范围。
- 只记录复现和解释当前结果所需的信息，不自行建设实验管理系统。

涉及文献与方法探索时，采用“阅读提出问题 → 证据形成假设 → 实验验证 → 失败案例界定边界 → 汇报形成结论”的工作方式。

使用 NVIDIA GPU 进行训练、推理或批量生成前，必须检查 `nvidia-smi` 的显存、利用率和进程，在已分配或获准使用的 GPU 中显式选择设备，不能仅凭空闲判断使用权限。不得结束其他用户或来源不明的进程。

## 5. 文件组织与文档分工

以当前打开的 Git 仓库为项目根目录，不依赖固定盘符或用户名。

| 文件或目录 | 职责 |
|---|---|
| `README.md` | 项目简介、文件导航、实际运行方式 |
| `AGENTS.md` | 项目范围、长期协作规则、已确认方案和实施策略 |
| `项目进度.md` | 当前状态、讨论分析、方案评价、报告素材、结果和下一步 |
| `实验要求.pdf` | 原始作业要求 |
| `src/` | 正式实现代码 |
| `results/` | 必要的结果表和图 |
| `report/` | 作业报告及交付材料 |
| `data/` | 本地数据，默认不提交原始数据集 |
| `历史版本/` | 覆盖更新前的交付文件备份，默认仅本地保留 |

目录在实际需要时建立，不为凑齐结构创建空目录或占位文件。正式运行代码不能放入临时目录，项目文件不散落到桌面或其他无关位置。

### 5.1 文档维护与项目接续

- 用户明确指定的例外：本文件第 11 节保存 n3000 实验的报告分析素材；它属于有适用范围的结果分析，不作为新的算法规则。其他实时进度仍统一放在 `项目进度.md`。
- `AGENTS.md` 只保存长期规则、已经确认且相对稳定的方案、参数和实施策略。
- `AGENTS.md` 不保存方法优劣分析、实验假设、报告讨论素材、阶段性结果、临时评价或单轮实验流水。
- `项目进度.md` 是唯一实时进度入口，保存当前阶段、分析判断、方案评价、报告素材、实际结果、待确认问题和下一步。
- 开始任务时，按需阅读进度及其指向的文件，确认当前阶段、已确认选择和下一步，不因对话上下文缺失从头重做。
- 在任务完成、重要结果产生或安排改变后更新 `项目进度.md`，不为每条命令追加记录。
- 规则与固定实施方案以 `AGENTS.md` 为准；任务完成情况以实际代码、输出及验证记录为依据，发现进度描述不符时及时纠正。
- 用户已明确要求创建或修改的文档可直接处理；用户要求先审阅时，先提供草稿，确认后再写入。

### 5.2 文件迭代与备份

同一交付文件持续修改时，保持正式路径和文件名稳定。不得因修改而不断生成 `_简洁版`、`_修改版`、`_最终版` 等并行文件。

对已有报告、PPT、图表等交付文件进行覆盖更新前，先将旧文件备份到项目根目录 `历史版本/` 下对应的文件子目录，备份文件名附加日期时间；随后将新内容写回原正式路径。备份目录默认仅本地保留，由 `.gitignore` 排除。

一次连续修改任务在首次覆盖前备份一次，不为每次保存或内部调整重复备份。不同实验条件产生的独立结果应按实验含义命名，不互相覆盖。

源码、配置、`README.md`、`AGENTS.md` 和 `项目进度.md` 的日常修改由 Git 管理历史，不逐次建立文件副本。用户明确要求备份某个文件时，按要求执行。

## 6. 沟通与阶段讲解

- 默认使用中文，用户正在逐步建立表示学习、降维与聚类知识，不直接堆叠术语。
- 术语首次出现时，按“通俗解释 → 中文专业名称 → 英文全称 → 缩写 → 在本项目中的作用”介绍；没有必要时不强行补齐英文和缩写。
- 解释方法时说明输入、输出、核心思路、解决的问题、局限及与作业的关系。
- 推荐论文或代码时说明重要程度、阅读难度、重点内容、开源状态和使用成本，帮助理解方法之间的联系。
- 阶段复盘按照“目标 → 完成工作 → 结果证据 → 结论与限制 → 下一步及成功标准”说明。
- 区分已验证事实、根据证据作出的判断、未验证设想和计划，不将计划中的创新描述为已实现成果。
- 用户要求分步骤教学时，每次只讲一个主题，允许暂停、追问或调整深度。
- 对已授权的工作持续推进，不重复请求确认；关键要求缺失时再询问。
- 完成后简洁说明做了什么、如何验证、验证结果及仍存在的明确问题。

## 7. 论文阅读与总结

阅读论文前先介绍问题、方法和输入输出，按实际贡献说明创新点及对比方法，指出重点章节、阅读优先级、与作业的关系和潜在局限，不为凑数量罗列创新点。

优先阅读摘要、方法总览、表示构建、降维或聚类方法、关键设计、实验、消融和失败案例等与当前问题直接相关的内容。

逐段精读采用：

1. 原意翻译：忠实表达原意。
2. 大意梳理：说明该段在全文中的作用。
3. 概念拆解：用直觉、流程或例子解释关键概念。

用户可以直接跳到代码或具体问题。总结覆盖动机、方法流程、关键概念、创新点、核心设计、实验、局限和可借鉴内容；尚未读完时先简记到进度或明确的临时位置，不自动为每篇论文建立重复笔记体系。

## 8. 文件安全与运行环境

- 禁止批量或递归删除文件和目录，不使用 `del /s`、`rd /s`、`rmdir /s`、`Remove-Item -Recurse`、`rm -rf` 等命令。
- 删除前核实目标，每次只能删除一个明确绝对路径的普通文件。
- 不使用通配符或未解析的环境变量、命令替换确定删除目标。
- PowerShell 删除使用 `Remove-Item -LiteralPath` 指定明确绝对路径。
- 需要批量删除或删除目录时，停止并要求用户手动处理。
- 不擅自覆盖原始材料、重要结果或用户已有修改。

Windows 默认使用 PowerShell 7（`pwsh.exe`）。首次执行终端命令前检查 `$PSVersionTable.PSVersion`，主版本必须不低于 7；不可用时报告问题，不回退到 PowerShell 5.1。

## 9. Git 与 GitHub

仓库地址：`https://github.com/mengmcy1/cifar10-clustering`

- 用户明确要求“提交”时，执行 `commit + push`，无需重复确认。
- 提交前查看变更范围，确保只包含当前任务相关内容。
- 提交代码、必要配置、项目文档和适合入库的结果。
- 原始数据集、虚拟环境、缓存、模型权重、大型中间产物和交付文件备份默认通过 `.gitignore` 排除。
- 不提交密码、令牌、私钥或包含认证信息的远程地址。
- 使用已有 GitHub 认证；认证失败时明确报告，不输出或保存令牌。
- 未经明确授权，不强制推送或重写 Git 历史。
- 推送后检查远程同步状态，失败时如实记录，不将本地提交等同于已推送。

## 10. 已确认方案与实施策略

本节仅记录已经确认并作为后续实现基线的方案与参数。方法评价、选择理由、实验假设、报告分析素材和实际结果统一记录在 `项目进度.md`。

### 10.1 数据子集

- 使用 CIFAR-10 的 5 个类别：`airplane`、`automobile`、`frog`、`cat`、`dog`。
- 每类取 600 张，共 3000 张；若实际数据加载出现明确限制，再据实调整。
- 全项目固定随机种子 `random_state=42`。
- 导出样本清单 CSV。
- Raw Pixel、HSV Histogram、HoG 三种表示必须使用完全相同的样本集合。
- 真实类别标签不参与特征构建、PCA 拟合或 k-means 聚类，只在聚类完成后用于结果解释和辅助分析。

### 10.2 三种表示

三种表示分别独立完成后续 PCA 与聚类，不在主实验开始时进行特征拼接。

#### Raw Pixel

- RGB 图像尺寸为 `32×32×3`。
- 直接展平为 3072 维向量。
- 像素值由 `0~255` 缩放到 `[0,1]`。
- 不额外使用图像增强、滤波或 `StandardScaler`。

#### HSV Color Histogram

- RGB 转 HSV。
- H、S、V 三个通道分别使用 32 个 bins。
- 三个通道拼接为 96 维特征。
- 每个通道分别做 L1 归一化后再拼接。
- 不额外使用 `StandardScaler`。

#### HoG

- 先将 RGB 图像转换为灰度图。
- 参数固定为：`orientations=9`、`pixels_per_cell=(4,4)`、`cells_per_block=(2,2)`、`block_norm='L2-Hys'`。
- HoG 输出维度以实际库结果为准，不在代码中硬编码预估维度。
- 不额外使用 `StandardScaler`。
- 不主动扫描额外 HoG 参数组，除非实际结果暴露明确问题或用户另行要求。

### 10.3 PCA

- Raw Pixel、HSV Histogram、HoG 分别独立拟合 PCA。
- 每种表示直接从自己的特征空间分别拟合 `PCA(n_components=50)` 与 `PCA(n_components=2)`。
- PCA-50D 用于正式聚类；PCA-2D 用于可视化。
- 不采用“先 PCA-50D，再从 50D 降到 2D”的串联方式。
- 不额外手工做均值中心化。
- PCA 使用 `whiten=False`。
- 记录 PCA-50 的累计解释方差比例，但不据此改变作业指定的 50 维。
- 主聚类实验不额外划分监督学习意义上的 train/test。

### 10.4 k-means

- 对每一种表示的 PCA-50D 特征分别执行 k-means。
- 扫描 `k=2~12`。
- 固定参数：`init='k-means++'`、`n_init=20`、`random_state=42`。
- 其余参数优先采用 sklearn 默认值，不额外做参数搜索。
- 每个 k 至少记录 SSE（`inertia_`）和 Silhouette。
- 使用 SSE 肘部曲线与 Silhouette-k 曲线共同确定该表示的最终 k。
- 三种表示使用相同的 k 扫描范围和 k-means 参数，但允许根据各自曲线选择不同的最终 k。
- 不因为真实类别数量为 5 而强制使用 `k=5`。

### 10.5 最终评价与输出

- 最终评价至少包含：Silhouette、Calinski-Harabasz（CH）、Davies-Bouldin（DB）。
- 记录单次聚类运行时间与内存。
- 输出 PCA-2D 聚类散点图。
- 每个最终簇展示 3 张最接近簇中心的代表样本。
- 按作业要求，可用 2~3 个样本量点观察运行时间和内存变化；实现保持简单，不建立额外 benchmark 系统。

### 10.6 Plus 阶段实施顺序

- 先完整完成 Basic 主流程、结果输出与必要分析，再进入 Plus 阶段；Basic 未完成前不提前展开 Plus 的具体参数设计。
- Plus 阶段计划分别尝试作业给出的三个方向：替代聚类、轻量自编码器表示、多随机种子稳定性。
- 三个方向的具体算法、网络结构、参数和实验设置，待 Basic 真实结果产生后再统一讨论确定，不在当前阶段提前锁死。
- 三个方向完成后，根据实际结果、可解释性和报告价值决定正式报告重点展开哪一项；其他结果可按实际价值作为补充，不为凑内容强行展开。

### 10.7 尚未确定

以下事项尚未确认，不得擅自固定：

- 主实验完成后是否增加三种特征拼接实验。
- 时间与内存缩放最终采用 2 个还是 3 个样本量点。
- Plus 三个方向的具体实现方案与参数。

## 11. n3000 实验报告分析素材

本节按用户明确要求保存，供后续写报告引用。记录日期：2026-09-13。以下结果仅适用于当前样本、表示参数、PCA 和 k-means 设置；不能将单轮结果写成跨样本、跨随机种子的一般结论。具体数值以对应 CSV 为准，正文百分比按展示精度四舍五入。

### 11.1 实验范围与证据入口

- CIFAR-10 train split 中选取 airplane、automobile、frog、cat、dog，每类 600 张，共 3000 张；随机种子 42，三种表示使用同一份样本清单。
- 分别构建 Raw Pixel、HSV Histogram、HoG，并各自独立拟合 PCA-50D 用于聚类、PCA-2D 用于展示。k-means 使用 k-means++、n_init=20、random_state=42，扫描 k=2～12。
- 真实标签用于按指定类别抽样，以及聚类后的类别组成解释；不进入表示构建、PCA 或 k-means 拟合。本节的类别分析是事后解释，不是监督训练结果。
- 样本清单：[sample_list.csv](results/n3000/sample_list.csv)。最终汇总：[final_metrics.csv](results/n3000/final_metrics.csv)。
- Raw：[逐簇统计](results/n3000/raw_pixel/all_k_cluster_composition.csv)、[二维对比图](results/n3000/raw_pixel/pca2_all_k.png)。
- HSV：[逐簇统计](results/n3000/hsv_histogram/all_k_cluster_composition.csv)、[二维对比图](results/n3000/hsv_histogram/pca2_all_k.png)。
- HoG：[逐簇统计](results/n3000/hog/all_k_cluster_composition.csv)、[二维对比图](results/n3000/hog/pca2_all_k.png)。
- 每种表示目录中的 `k_scan.csv/png` 提供选 k 依据，`representatives.png/csv` 和 `cluster_composition.csv` 对应最终 k=2。全 k 补充阶段只生成二维图和组成统计，没有生成 k>2 的代表图片。
- 已核验：每份全 k 统计表有 77 行（2+3+…+12）；每个 k 的簇大小之和为 3000，各类别之和为 600；补充阶段样本清单与原实验一致，各 k 的 SSE 与原扫描在相对容差 1e-5 内一致。全 k 范围内最小簇分别为 Raw 156 张、HSV 60 张、HoG 160 张。

### 11.2 逐簇统计的阅读口径

每行表示某一个 k 下的一个簇。`size` 为该簇总数，`fraction=size/3000` 为该簇占全部样本的比例；后五列为该簇内五个真实类别的数量。

必须区分三个分母。以 HoG 的 k=4、簇 0 为例：649 张中有飞机 341 张。

| 比例 | 计算结果 | 含义 |
|---|---|---|
| 簇占全部样本比例 | 649/3000=21.6% | 该簇有多大，即 fraction |
| 飞机在簇内的比例 | 341/649=52.5% | 该簇的飞机集中程度 |
| 该簇覆盖全部飞机的比例 | 341/600=56.8% | 全部飞机中有多少落在此簇 |

局部类别集中不等于整个类别被完整收集。例如一个簇 70% 是飞机，也可能只覆盖全部飞机的 20%。报告需同时关注簇内混杂与类别分散，不只挑选占比最高的一行。

原始数据五类各占 20%；动物（三类）合计占 60%，交通工具（两类）合计占 40%。解释动物或交通工具富集时必须考虑这个起点，不能把动物占 61% 与占 93% 视为同等强度的证据。

簇编号是任意编号：同一 k 下不同表示的簇 0 不保证对应；同一表示不同 k 下的簇 0 也不保证对应。k-means 每次独立拟合，不是层次聚类，不能仅凭编号或相似类别数量断言某个簇由上一轮某簇拆分而来。

### 11.3 主结果、选 k 与 PCA 的解释

| 表示 | 原特征维度 | PCA-50 累计解释方差 | 最终 k | Silhouette | CH | DB |
|---|---:|---:|---:|---:|---:|---:|
| Raw Pixel | 3072 | 85.11% | 2 | 0.2110 | 948.94 | 1.7025 |
| HSV Histogram | 96 | 97.18% | 2 | 0.3600 | 539.30 | 1.1898 |
| HoG | 1764 | 36.97% | 2 | 0.0925 | 295.15 | 3.0662 |

三者在本次 k=2～12 扫描中均于 k=2 取得最高平均轮廓系数。Raw 的 SSE 在 k≈4 附近有一定转折，HSV 与 HoG 的转折较平缓；肘部判断不唯一。综合两条曲线，最终均选 2，但不能写成“两项指标一致证明 k=2 最优”，也不能写成“数据只存在两个真实类别”或“二分结构最稳健”。稳定性尚未通过多随机种子或重抽样验证。

Silhouette 同时反映样本与本簇、最近其他簇的相对距离，通常越高越好；CH 比较簇间与簇内离散程度，通常越高越好；DB 比较簇内松散程度与簇间距离，通常越低越好。本轮 HSV 的 Silhouette、DB 更好，Raw 的 CH 更高，不能宣称三个指标一致支持 HSV。不同表示改变了距离空间，这些指标不直接等同于语义类别识别能力。

PCA-50 累计解释方差是前 50 个主成分的方差之和占原特征总方差的比例。它反映方差保留程度，不是准确率或类别信息保留比例。HSV 原本只有 96 维，其比例高不能单独证明表示更好；HoG 舍弃较多方差，但是否因此导致内部指标偏低，尚无单因素实验验证。背景、亮度的变化也可能具有很大方差，PCA 不看类别标签，不会自动优先保留语义差异。

### 11.4 Raw Pixel：多数簇混杂，局部飞机群体较集中

k=2 时，两个簇大小为 1210、1790。簇 0 含飞机 452 张，占全部飞机的 75.3%，但飞机仅占该簇 37.4%；簇 1 中动物占 70.7%，仍有 525 张交通工具图片。飞机与汽车并未整体聚到同一簇，因此不宜把该划分直接命名为交通工具与动物。

k=4 的实际组成如下：

| 簇 | 总数 | 飞机 | 汽车 | 青蛙 | 猫 | 狗 | 最多类别及簇内占比 |
|---|---:|---:|---:|---:|---:|---:|---|
| 0 | 834 | 89 | 97 | 173 | 198 | 277 | 狗，33.2% |
| 1 | 683 | 188 | 225 | 83 | 113 | 74 | 汽车，32.9% |
| 2 | 926 | 66 | 184 | 280 | 204 | 192 | 青蛙，30.2% |
| 3 | 557 | 257 | 94 | 64 | 85 | 57 | 飞机，46.1% |

四簇虽有不同类别倾向，但没有单一类别占比超过一半的簇。SSE 的转折不等于形成了四个明确的语义类别。k=5 也没有恢复五类：两个簇均为飞机数量最多，其他分别为青蛙、狗、汽车最多，且每簇单一类别占比均不足 50%。

增大 k 后可出现较集中的飞机小簇。例如 k=11、簇 9 的 156 张中有 115 张飞机（73.7%），但只覆盖全部飞机的 19.2%；k=12、簇 1 的 184 张中有 132 张飞机（71.7%），覆盖飞机的 22.0%。这支持“找到了局部飞机群体”，不支持“飞机类别已完整分离”。

最终 k=2 的代表图片中，簇 0 相对偏亮，簇 1 的三张代表图片均为背景偏暗、偏绿的青蛙，可提示亮度或背景因素的作用；这只是少量代表图片的视觉观察，尚不能概括全簇，更不能解释未展示图片的其他 k。

### 11.5 HSV：大小不均衡，细分后出现更多局部语义倾向

k=2 时簇大小为 215、2785，小簇占 7.2%，大簇占 92.8%。大簇中的飞机、汽车、青蛙、猫、狗分别为 533、538、578、557、579 张，最大类别比例仅 20.8%，接近原始单类比例 20%。所以较高轮廓系数对应的是一个小群体与大群体的几何划分，不能视为物体类别已经分开。

小簇的三张代表图片均有大面积浅色、近白色背景，支持提出“特殊颜色分布参与划分”的解释。没有全簇颜色统计，不能断言整个簇均为白背景，也不能将小簇一概称为异常点或噪声。簇大小不均衡本身不是程序错误。

k=4 的组成如下：

| 簇 | 总数 | 飞机 | 汽车 | 青蛙 | 猫 | 狗 |
|---|---:|---:|---:|---:|---:|---:|
| 0 | 1594 | 238 | 415 | 306 | 305 | 330 |
| 1 | 191 | 63 | 53 | 16 | 40 | 19 |
| 2 | 420 | 267 | 96 | 10 | 23 | 24 |
| 3 | 795 | 32 | 36 | 268 | 232 | 227 |

簇 2 飞机占 63.6%，交通工具合计占 86.4%；簇 3 动物占 91.4%，但三类动物仍混合。簇 0 占全部样本 53.1%，仍是大混合簇。因此 k=4 的语义探索价值已有统计支持，但不能称为四类被清晰恢复。

k=7 的轮廓系数为 0.1411，是 k≥3 范围内的最高点，但仍明显低于 k=2 的 0.3600。簇 0 的 180 张中有 128 张飞机（71.1%），簇 4 的 219 张中有 136 张飞机（62.1%）；簇 5 的青蛙占 45.1%，簇 6 的动物合计占 90.3%。同时最大簇仍有 1233 张（41.1%）且五类混杂。

多个飞机集中的簇提示飞机图片可能对应多种颜色模式，但不能仅据计数命名其背景、姿态或认定跨 k 的拆分关系。到 k=12，依然存在多个飞机集中簇和猫狗等动物混合簇，没有形成一簇一类别的整体结构。

### 11.6 HoG：粗粒度区分明显，猫狗细分仍困难

k=2 时，簇 0 有 1884 张，其中动物 1605 张（85.2%）；簇 1 有 1116 张，其中交通工具 921 张（82.5%）。从每类去向看：

| 类别 | 主要对应簇 | 进入该簇的数量 | 占该类别 600 张的比例 |
|---|---:|---:|---:|
| 飞机 | 1 | 436 | 72.7% |
| 汽车 | 1 | 485 | 80.8% |
| 青蛙 | 0 | 524 | 87.3% |
| 猫 | 0 | 531 | 88.5% |
| 狗 | 0 | 550 | 91.7% |

簇内组成与类别去向两个角度共同支持“与动物/交通工具粗粒度语义存在较明显对应”。这是使用标签进行的事后解释，不是监督分类准确率，也没有恢复五个类别。

k=4 的组成如下：

| 簇 | 总数 | 飞机 | 汽车 | 青蛙 | 猫 | 狗 |
|---|---:|---:|---:|---:|---:|---:|
| 0 | 649 | 341 | 266 | 9 | 17 | 16 |
| 1 | 830 | 46 | 10 | 94 | 306 | 374 |
| 2 | 754 | 99 | 146 | 260 | 132 | 117 |
| 3 | 767 | 114 | 178 | 237 | 145 | 93 |

簇 0 的交通工具占 93.5%，但飞机和汽车混合；簇 1 的猫狗合计占 81.9%，但猫狗并未分开。簇 2、3 都是青蛙数量最多，青蛙分别只占 34.5%、30.9%，仍有明显混杂。

k=5 同样没有恢复五类：两个簇均以青蛙数量最多，另有猫狗集中簇和交通工具集中簇。将 k 设为真实类别数，不等于自动获得真实类别。

k=12 时，簇 8 的 160 张中有汽车 124 张（77.5%），但只覆盖全部汽车的 20.7%；簇 11 的 298 张中有青蛙 162 张（54.4%），只覆盖全部青蛙的 27.0%。这些是局部类别集中，不是类别的完整恢复。

特别值得引用的是 k=12、簇 2：199 张中有猫 94 张、狗 88 张，猫狗合计占 91.5%，二者仍近乎各半。这支持“当前流程较容易聚集猫狗的共同视觉模式，但细分猫狗存在困难”，不支持“已经识别猫和狗”。具体共同视觉因素仍需额外图片证据，不能直接归因于某个轮廓或姿态。

### 11.7 跨 k 的共同现象与比较边界

1. **局部集中与类别分散同时发生。** 较大 k 下存在单类占比较高的小簇，但它们往往只覆盖该类别的一小部分。不能只报最好的簇而忽略大混合簇及同类分散。
2. **猫狗是明确的困难点。** 检查本次全部 k 的统计，猫在任一簇内的最高占比分别为 Raw 28.5%（k=12、簇 8，97/340）、HSV 32.7%（k=5、簇 2，179/548）、HoG 47.2%（k=12、簇 2，94/199）。三者均未产生猫占比超过一半的簇；HoG 多次形成猫狗共同集中的簇。上述为观测范围内的描述性事实，不是统计显著性或稳定性检验。
3. **内部指标不等同语义对应。** HoG 的内部指标低于其他流程，但 k=2 的粗粒度类别对应较明显；HSV 的轮廓系数最高，但 k=2 保留了 92.8% 样本的大混合簇。应结合内部指标、簇大小和类别组成解释，不能只用单一分数排名。
4. **比较对象是完整流程。** 不同表示及其各自 PCA 改变了距离空间；只能说当前“表示+PCA-50+k-means”下观察到某结果，不能直接推断颜色信息或形状信息本身孰优孰劣。
5. **二维展示存在投影限制。** 同一表示各 k 固定二维坐标，颜色是 50 维聚类标签。二维重叠不等于 50 维完全重叠，整齐边界也不等于有天然间隙；不同 k 的颜色不追踪同一簇。
6. **方差与语义不同。** HoG 的 PCA-50 方差保留较低，同时仍有粗粒度语义对应；这说明不能仅凭解释方差比例判断类别划分效果，但不证明丢弃方差有益。

### 11.8 是否改变最终 k 与未完成事项

主方案保持三种表示 k=2。理由是本次扫描中三者平均轮廓系数均在 2 最高，SSE 肘部证据不足以提供一致的其他选择。较大 k 的局部类别集中作为补充讨论，不根据真实标签事后悄悄更换原先使用内部指标确定的主方案。

k=4、7、12 的统计可分别用于说明初步细分、局部指标峰值或更高粒度下的类别碎片化；不需要逐行复述全部 231 行统计，也不能据少数例子宣称其他 k 都没有价值。

目前没有跨随机种子、重抽样的验证，不能使用“稳定”“稳健”“显著优于”等措辞。背景和颜色原因尚主要依据最终 k=2 的代表图观察。样本量缩放未完成，Plus 未开始，不将本轮分析包装成已完成全部作业。

最终聚类计时约为 Raw 0.173 秒、HSV 0.190 秒、HoG 0.164 秒，仅对应模型构造与拟合，不包括特征、PCA 或完整扫描。RSS 峰值增量约为 1.262、0.500、0.117 MiB，受前序内存复用与 10 ms 采样影响，不等于算法实际内存需求；单次耗时差异及很小的内存增量不足以建立稳定的效率排名或规模趋势。

### 11.9 可用于报告的综合表述

> 在固定的 3000 张 CIFAR-10 子集上，三种表示均在 k=2 时取得扫描范围内最高的平均轮廓系数，但其簇组成具有明显差异。Raw Pixel 的簇中存在较多类别混合；HSV 的 k=2 划分形成一个 215 张的小簇和一个 2785 张的大簇，较高内部指标未直接对应物体类别的分离；HoG 虽然平均轮廓系数较低，但两个簇分别以动物和交通工具为主，显示出较明显的粗粒度语义对应。

> 增大 k 后，部分簇的类别集中程度提高，但同类样本也分散到多个簇，未形成与五个真实类别一一对应的整体划分。HSV 出现飞机集中的群体及动物集中的群体，同时保留较大的混合簇；HoG 出现交通工具集中的簇与猫狗共同集中的簇，但猫狗的细粒度区分仍然困难。这表明内部几何指标、局部类别集中程度与类别整体恢复是不同的评价角度，应结合逐簇组成和可视化共同解释。

> PCA-50 对 Raw Pixel、HSV 和 HoG 的累计解释方差比例分别为 85.11%、97.18% 和 36.97%，表明固定 50 维对不同表示的方差保留程度存在差异。解释方差不直接衡量类别信息或聚类质量，不能据此单独判断表示优劣。上述结论仅适用于本次样本和参数设置，尚未通过多随机种子或重抽样验证。
